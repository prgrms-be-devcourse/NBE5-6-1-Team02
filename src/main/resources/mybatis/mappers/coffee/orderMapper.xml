<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="coffeemanager.app.model.coffee.CoffeeRepository">
  <!-- 1. 주문건 저장 -->
  <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO orders (email, address, postcode, total_price, order_date)
    VALUES (#{email}, #{address}, #{postcode}, #{totalPrice}, #{orderDate})
  </insert>

  <!-- 2. 주문-커피 매핑 저장 -->
  <insert id="insertOrderCoffee">
    INSERT INTO ordercoffee (order_id, coffee_id, quantity)
    VALUES (#{orderId}, #{coffeeId}, #{quantity})
  </insert>


</mapper>